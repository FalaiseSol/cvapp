import React from 'react';
import {
  View, Text,
} from 'react-native';

const Sandbox = (): JSX.Element => {
  return (
    <View>
      <Text>
        Sandbox
      </Text>
    </View>
  );
};

export default Sandbox;